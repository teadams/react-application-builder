-- app
-- body
  
-- menu - 
    -- menu_title  &&& (props:menu_title, menu_description) (change name to component_inputs?)
  
--- list
      header_wrap 
           header (&&&) (prop:list_title, prop:list_description, defaults to pretty name of object)
      list_wrap (Table) (onClick)
        list_header_wrap 
            list_header (&& the pretty names of the forms)
        body_wrap (TableRow)
            list  (RenderACSList)
        footer_wrap (Fragment)
            footer
    -- other Props  
        -- data
        -- object_type
        -- api_options
  

--- row (RenderACSRow)
  (Controller)  header_wrap (&&&,Fragment)
       header  (prop:row_title, row_description)
  (Renderer)  section_wrap (&&&, Fragment)
      section_header (prop:section_title, section_description)
         row_wrap (onClick)
              field_chunk_wrap (&&&, Fragment) (prop:fields_per_chunk (2,1))
                row (&&)
      -- other Props  
            -- data
            -- object_type
            -- field_list, sections?
            -- id
            -- api_options
  
--    field: field_wrap (onFieldClick)
        field (field_component/field_component_name)
          Props:
            -- display (value, name_colon_value, name_value_cells) (Renderer)
            - click_to_edit (used in controller)
            - mouse_over_to_edit 
            - editable
            - form - (form can be at field or row level)
            - mode (view, text, edit) (passed through Renderer to View) - initial value is always passed to controller, then state is managed in the controller
            - data
            - field_model 
            - valid_values (specified by model or from select_list)
            - api_options
            - view_component = Text, Select